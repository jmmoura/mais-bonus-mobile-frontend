import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'mais-bonus-mobile-frontend',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
